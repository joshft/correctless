name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    name: Tests (jq ${{ matrix.jq_version }})
    runs-on: ubuntu-latest
    # AP-011: Matrix across jq versions to catch operator precedence and
    # other version-portability bugs before they hit main. See PMB-001.
    # Minimum supported is jq 1.7 (Ubuntu 24.04 LTS). jq 1.6 has known
    # incompatibilities and is not supported — see ENV-002.
    strategy:
      fail-fast: false
      matrix:
        jq_version: ["1.7.1", "1.8.1"]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Install jq ${{ matrix.jq_version }}
        run: |
          case "${{ matrix.jq_version }}" in
            "1.7.1")
              url="https://github.com/jqlang/jq/releases/download/jq-1.7.1/jq-linux-amd64"
              ;;
            "1.8.1")
              url="https://github.com/jqlang/jq/releases/download/jq-1.8.1/jq-linux-amd64"
              ;;
            *)
              echo "Unknown jq version: ${{ matrix.jq_version }}" >&2
              exit 1
              ;;
          esac
          sudo curl -fsSL "$url" -o /usr/local/bin/jq
          sudo chmod +x /usr/local/bin/jq
          jq --version

      - name: Configure git identity for tests
        run: |
          git config --global user.email "ci@correctless.dev"
          git config --global user.name "CI"

      - name: Run tests
        run: |
          for f in tests/test-*.sh; do
            [[ "$f" == */test-helpers.sh ]] && continue
            echo ">>> $f"
            bash "$f" || exit 1
          done

      - name: Verify sync
        run: |
          bash sync.sh
          git diff --exit-code || (echo "sync.sh produced changes — run sync.sh and commit" && exit 1)

  sfg-lift-check:
    name: SFG lift-and-restore final-state backstop
    runs-on: ubuntu-latest
    # CS-018a / AP-037: unconditional, dedicated job (no matrix, not inside the
    # tests/test-*.sh loop). Fails if a .correctless/.sfg-lift-active sentinel is
    # still committed (lift commit landed without its restore commit).
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Check no pending SFG lift sentinel
        run: bash scripts/check-no-pending-sfg-lift.sh

  test-suite:
    name: Test Suite
    runs-on: ubuntu-latest
    needs: [test, sfg-lift-check]
    if: always()
    steps:
      - name: Check matrix results
        run: |
          if [ "${{ needs.test.result }}" != "success" ]; then
            echo "Test matrix failed: ${{ needs.test.result }}"
            exit 1
          fi
          if [ "${{ needs.sfg-lift-check.result }}" != "success" ]; then
            echo "SFG lift-check failed: ${{ needs.sfg-lift-check.result }}"
            exit 1
          fi

  shellcheck:
    name: ShellCheck
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0

      - name: Run ShellCheck
        uses: ludeeus/action-shellcheck@00cae500b08a931fb5698e11e79bfbd38e612a38 # v2.0.0
        with:
          scandir: ./hooks
          additional_files: sync.sh setup scripts/lib.sh scripts/antipattern-scan.sh
          severity: warning
